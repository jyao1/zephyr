# SPDX-License-Identifier: Apache-2.0

config BOARD_UEFI_PAYLOAD_IA32
	bool "UEFI PAYLOAD IA32"
	depends on SOC_IA32
	select QEMU_TARGET
	select HAS_COVERAGE_SUPPORT

config BOARD_UEFI_PAYLOAD_X64
	bool "UEFI PAYLOAD X64"
	depends on SOC_IA32
	select QEMU_TARGET
	select X86_64

